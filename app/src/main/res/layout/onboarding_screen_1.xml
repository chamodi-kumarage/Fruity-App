<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_onboad_gradient"
    android:padding="24dp">

    <!-- Illustration (centered at top) -->
    <ImageView
        android:id="@+id/imgIllustration"
        android:layout_width="385dp"
        android:layout_height="347dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"
        android:scaleType="centerInside"
        android:src="@drawable/fu1" />

    <!-- Logo text image instead of title -->
    <ImageView
        android:id="@+id/imgLogoText"
        android:layout_width="139dp"
        android:layout_height="56dp"
        android:layout_below="@id/imgIllustration"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="42dp"
        android:contentDescription="App Logo Text"
        android:src="@drawable/name" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/txtSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/imgLogoText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="Get your fresh fruits easily\nanytime, anywhere"
        android:textColor="#666666"
        android:textSize="16sp" />

    <!-- Next arrow button -->
    <ImageView
        android:id="@+id/imageView14"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="11dp"
        android:layout_marginBottom="21dp"
        android:background="@drawable/bg_next_circle"
        android:contentDescription="Next"
        android:padding="12dp"
        android:src="@drawable/ic_next_arrow" />

</RelativeLayout>
