<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- top (green) → center (creamy) → bottom (orange) -->
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="@color/accent_green"
        android:centerColor="@color/secondary_creamy_white"
        android:endColor="@color/primary_orange"
        android:centerY="0.5" />
</shape>
