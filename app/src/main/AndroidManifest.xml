<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fruitiee">

        <!-- Splash / Launch -->
        <activity
            android:name=".LaunchScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding flow -->
        <activity android:name=".Onboarding1Activity" />
        <activity android:name=".Onboarding2Activity" />
        <activity android:name=".Onboarding3Activity" />

        <!-- Auth -->
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".OtpSendActivity" />

        <!-- Home -->
        <activity android:name=".AllActivity" />
        <activity android:name=".OrganicActivity" />
        <activity android:name=".SaladActivity" />
        <activity android:name=".JuiceActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".EditOrderDetailsActivity" />
        <activity android:name=".LiveLocationActivity" />
        <activity android:name=".LiveLocationConfirmActivity" />
        <activity android:name=".PaymentOptionsActivity" />
        <activity android:name=".CheckoutPaymentActivity" />
        <activity android:name=".CheckoutSuccessActivity" />
        <activity android:name=".PaymentReceiptActivity" />
        <activity android:name=".OrderTrackingActivity" />
        <activity android:name=".PapayaDetailActivity" />
        <activity android:name=".FruitBasketActivity" />
        <activity android:name=".NotificationsActivity" />
        <activity android:name=".FavouriteActivity" />
        <activity android:name=".MyOrderActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".PrivacyPolicyActivity" />
        <activity android:name=".ContactUsActivity" />
        <activity android:name=".ProfileAnalyticsActivity" />
        <activity android:name=".BasketCartActivity" />
        <activity android:name=".ContactSuccessActivity" />

    </application>
</manifest>
